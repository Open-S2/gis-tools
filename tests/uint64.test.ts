// import Uint64Generator from '../src/wasm/uint64';

// import { expect, test } from 'bun:test';

// test('S2Cell creation', () => {
//   const gen = new Uint64Generator();

//   const pointA = gen.fromFaceST(0, 0.5, 0.5);
//   const pointB = gen.fromFaceST(1, 0.92, 0.65);
//   const pointC = gen.fromLonLat(0, 0);
//   // console.log(pointC, pointA);
//   const compare = pointA.compare(pointB);
//   const compare2 = pointA.compare(pointC);
//   expect(compare).toEqual(-1);
//   expect(compare2).toEqual(0);
// });
